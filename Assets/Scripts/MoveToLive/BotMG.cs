using DG.Tweening;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BotMG : MonoBehaviour
{
/*    public List<Transform> tfrmFloor = new List<Transform>();
    public List<GameObject> lisBot;
    Rigidbody rb;


    public void Awake()
    {
        DieuHuong();
        rb = GetComponent<Rigidbody>();
    }

    public void DieuHuong()
    {
        float offsetx = 0.1f;
        
        foreach (GameObject bot in  lisBot)
        {
            int ran = Random.Range(0, tfrmFloor.Count);
          
            bot.transform.DOMove(new Vector3(tfrmFloor[ran].position.x + offsetx , tfrmFloor[ran].position.y + 2.5f, tfrmFloor[ran].position.z), 9f);
           
            offsetx += 0.3f;
        }
    }*/
    
}
